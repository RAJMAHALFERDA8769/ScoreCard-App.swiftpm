
import Foundation
import SwiftUI

struct ResultsView: View {
    var body: some View {
        VStack {
            Text("Results:")
                       .font(.system(size: 30))
                       .font(.title)
                       .foregroundColor(.white)
                       .padding()
                       .background(Color.blue)
                       .cornerRadius(10)
                   Spacer()
            }
        }
    }
